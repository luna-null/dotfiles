# Add csharprepl command line options to this file to configure csharprepl.
# You may uncomment an option below by removing the leading '#' character.

# Reference assemblies, nuget packages, and csproj files. Can be specified multiple times.
# --reference <string[]>

# Add using statement. Can be specified multiple times.
# --using <string[]>

# Reference a shared framework.
# --framework <string>

# Read a theme file for syntax highlighting. Respects the NO_COLOR standard.
# --theme <string>

# Uses terminal palette colors for syntax highlighting. Respects the NO_COLOR standard.
# --useTerminalPaletteTheme <boolean>

# Formatted prompt string.
# --prompt <string>

# Use UTF8 output encoding and unicode character decorations (requires terminal support).
# --useUnicode <boolean>

# Allows prerelease NuGet versions when searching for the latest package version.
# --usePrereleaseNugets <boolean>

# If input is piped via stdin, evaluate it line by line instead of in one batch.
# --streamPipedInput <boolean>

# Produce a trace file in the current directory, for CSharpRepl bug reports.
# --trace <boolean>

# Width of tab character.
# --tabSize <int32>

# OpenAI API key. Alternatively, set the OPENAI_API_KEY environment variable.
# --openAIApiKey <string>

# OpenAI prompt to prefix to all code submissions
# --openAIPrompt <string>

# OpenAI model configuration
# --openAIModel <string>

# Maximum number of previous REPL entries to send to OpenAI as context.
# --openAIHistoryCount <nullable`1>

# OpenAI temperature configuration
# --openAITemperature <nullable`1>

# OpenAI top_p configuration
# --openAITopProbability <nullable`1>

# Key binding to trigger the completion list. Can be specified multiple times.
# --triggerCompletionListKeys <string[]>

# Key binding to insert a newline character. Can be specified multiple times.
# --newLineKeys <string[]>

# Key binding to submit the prompt. Can be specified multiple times.
# --submitPromptKeys <string[]>

# Key binding to submit the prompt with detailed output. Can be specified multiple times.
# --submitPromptDetailedKeys <string[]>

# Culture to use for access to the MSDN documentation. Defaults to the current culture.
# --culture <string>